server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        # SPA: любой путь без совпадающего файла отдаёт index.html,
        # маршрутизацией занимается React Router на клиенте.
        try_files $uri $uri/ /index.html;
    }
}
