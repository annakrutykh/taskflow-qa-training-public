"""Бизнес-логика, транзакции, аудит, доменные события (CLAUDE.md, раздел 5).

Один модуль на ресурс (auth, users, projects, tasks, comments, labels,
admin) — роутеры в app/api/v1/endpoints/ только парсят HTTP и вызывают
сюда, без прямых запросов к БД и без бизнес-правил.
"""
